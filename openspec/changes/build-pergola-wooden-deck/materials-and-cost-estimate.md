# Current Estimate

Use `nakupni-seznam.md` for current quantities, Czech suppliers, links, and prices.

Current estimate:

- Without optional oil: about `49,846 Kc`.
- With one 5 l oil can: about `51,574 Kc`.
- Delivery and base aggregate are excluded; verify compacted depth and `0/32` versus `2/32` before ordering.
- The estimate uses the conservative continuous 45 mm `4/8` layer; localized bedding would require less.

Old exploratory variants were removed to prevent accidental shopping from stale quantities.
